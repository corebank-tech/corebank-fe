node_modules
/.pnpm-store/
dist
dist-ssr
*.tsbuildinfo
.DS_Store
*.local

.claude/
CLAUDE.md
/.idea/
.serena

.env
.env.*
!.env.example

playwright-report/
test-results/
coverage/
.eslintcache

# orval 생성물 — 커밋하지 않는다. 각자 `pnpm codegen` 으로 만든다
src/shared/api/generated

# 스펙은 서버 api-docs 에서 직접 읽는다 (orval.config.ts)
openapi.yaml

*storybook.log
storybook-static


# 로컬 전용 문서
*.xlsx
*.xls
/docs/*
/prompt

# 예외 — 관리자 요구사항 정의서는 팀이 읽어야 한다 (#156).
# 1차 요구사항정의서에 관리자 화면이 0건이라 이 문서가 유일한 정본이다.
!/docs/requirements-admin.md

e2e/**/*-snapshots/
