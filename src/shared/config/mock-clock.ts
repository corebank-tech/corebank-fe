/**
 * 실 API 연동 전까지 화면 전반이 "지금"으로 취급하는 단일 mock 기준시각.
 * 화면마다 다른 값을 쓰면 기준일시·조회 가능 기간 등이 화면 간에 어긋난다 —
 * 새 mock 데이터를 추가할 때도 이 값을 기준으로 삼는다.
 */
export const MOCK_NOW = "2026-07-23T08:57:34"
export const MOCK_TODAY = "2026-07-23"
